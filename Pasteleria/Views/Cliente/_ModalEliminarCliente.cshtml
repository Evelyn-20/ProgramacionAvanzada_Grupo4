<!-- Modal Eliminar Cliente -->
<div class="modal fade" id="modalEliminar" tabindex="-1" aria-labelledby="modalEliminarLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="modalEliminarLabel">
                    <i class="fas fa-trash"></i> Eliminar Cliente
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-warning">
                    <i class="fas fa-exclamation-triangle"></i>
                    <strong>¿Estás seguro que deseas eliminar este cliente?</strong>
                    <br>Esta acción no se puede deshacer y se perderán todos los pedidos asociados.
                </div>

                <div class="mb-3">
                    <strong>Nombre:</strong> <span id="eliminar-nombre"></span><br>
                    <strong>Cédula:</strong> <span id="eliminar-cedula"></span><br>
                    <strong>Correo:</strong> <span id="eliminar-correo"></span><br>
                    <strong>Teléfono:</strong> <span id="eliminar-telefono"></span><br>
                    <strong>Estado:</strong> <span id="eliminar-estado"></span>
                </div>
            </div>
            <div class="modal-footer">
                <form id="formEliminar" method="post">
                    <input type="hidden" id="eliminar-id" name="IdCliente" />
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times"></i> Cancelar
                    </button>
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash"></i> Eliminar
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>